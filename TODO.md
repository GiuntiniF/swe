- Valutare la riaggiunta del metodo getCalories() nella WorkoutActivity con l'aggiunta delle calorie bruciate per esercizio
- Valutare la reintroduzione del massimale (1RM) dell'atleta per esercizio. Rimosso dalla SRS (era in PT-04 e PT-05) perché non modellato e perché con il carico effettivo per sessione non serve a determinare i pesi. Se reintrodotto: è un dato di allenamento raccolto dal PT, non un dato anagrafico; sta sull'Athlete per ciascun ExerciseType, con la data del rilevamento, e viene sovrascritto a ogni nuovo rilevamento. Servirebbe per prescrivere i carichi come percentuale invece che in valore assoluto.
- Valutare la reintroduzione delle progressioni (di carico, di volume, doppia progressione, di densità). Rimosse dalla SRS (erano in PT-04, PT-05 e PT-09) perché non modellate e perché fuori dallo statement, che ammette la variazione di peso e serie/ripetizioni da allenamento a allenamento senza indicare come ottenerla. Oggi la variazione è esplicita: il PT compila un AssignedExercise per ciascuna sessione. Una progressione servirebbe a generare quei valori invece di farli inserire a mano, che è il vero costo dell'assegnazione (con 3 blocchi da 7 sessioni sono 21 form da compilare). Se reintrodotta: è parte della scheda, vale per tutti gli atleti che la seguono, e va legata al singolo esercizio del blocco mensile. Da valutare insieme al massimale, perché la progressione di carico in percentuale lo presuppone.
- Rifare il diagramma dei casi d'uso (`docs/softwarereqspec/src/diagrams/useCaseDiagram.png` e `.svg`). Il sorgente editabile non è nel repo: l'SVG è un export di Excalidraw ma senza la scena incorporata, e il PNG non ha metadati, quindi va ridisegnato da zero o recuperato dall'Excalidraw locale. È il solo pezzo della SRS rimasto indietro rispetto ai template dei casi d'uso, e oggi la figura contraddice le sezioni che la seguono. Da correggere: gli attori si chiamano "Allenatore" e "Cliente" invece di PT e Atleta; compaiono casi d'uso che non esistono più ("Crea template scheda" — i template di scheda sono stati rimossi, PT-03 crea direttamente una Scheda —, "Recupero password", "Cancella"); "Rifiuta sessione" è un flusso alternativo di PT-06, non un caso d'uso a sé. Il diagramma va ridisegnato sull'elenco attuale (U-01, PT-01..PT-09, A-01..A-05) e con la terminologia normalizzata, dove l'incontro con il PT è un "appuntamento" e "sessione di allenamento" indica solo l'allenamento dentro la scheda.
- Rinominare i diagrammi obsoleti in `docs/softwarereqspec/src/diagrams/useCaseDiagrams/` (`assegnazioneScheda`, `flussoClienteV1`, `loginAllenatore`, `registrazioneCliente`, ciascuno in `.png` e `.svg`, più il sorgente `diagrams.excalidraw` che li contiene tutti e quattro). I nomi usano la terminologia superata ("Allenatore", "Cliente") e non dicono a quale caso d'uso si riferiscono, a differenza dei template dei casi d'uso rinominati con il prefisso dell'ID. Anche il contenuto è superato: parlano di template di scheda, "scheda Ad-Hoc", recupero password e del flusso della password temporanea. Nessun `.tex` li include, quindi la prima decisione è se tenerli: se servono come storico, il rename deve dire che sono bozze superate e a quale caso d'uso corrispondono (per esempio una sottocartella `bozze/` con il prefisso dell'ID); altrimenti si cancellano e il TODO si chiude.
